# Notes for 5/11/25

# For response what you can set

response.set_cookie(key, value, httponly, secure, samesite, max_age)
response.delete_cookie(key)
response.headers["X-Something"] = "value"
response.status_code = 201

# For request what you can see

def route(request: Request):
request.cookies.get("access_token")
request.cookies.get("session_id")
request.headers.get("Authorization")
request.headers.get("Content-Type")
request.method
request.url
request.client.host # IP address
await request.json() # raw body as dict
await request.body() # raw body as bytes

POST /auth/login HTTP/1.1
Host: api.example.com
Content-Type: application/json
Authorization: Bearer abc123
Cookie: session_id=xyz

{
"email": "eric@example.com",
"password": "secret"
}
